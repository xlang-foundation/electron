# XLangImportOptions Object

* `fromPath` string (optional) - XLang module search path used as the import's
  `from` location.
* `thru` string (optional) - XLang transport route used by the import. For
  example, `lrpc:9089` imports a module through an XLang local-RPC endpoint.
